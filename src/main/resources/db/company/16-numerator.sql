--liquibase formatted sql
--changeset piotrkorniak:16

CREATE TABLE $schema.NUMERATORY (
	ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
	KOD VARCHAR(32),
	NAZWA VARCHAR(128),
	MASKA VARCHAR(32),
	RODZAJ VARCHAR(1) NOT NULL,
---	TYPY CHAR(1),

	PRIMARY KEY (ID)
);